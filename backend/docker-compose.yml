services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_DIR=/app/models
      - CORS_ORIGINS=["http://localhost:3000"]
    volumes:
      - ../models:/app/models
    restart: unless-stopped